// Attach click handler to Buy Now button
document.getElementsByClassName('add').addEventListener('click', addButton);

// Show confirmation message when Buy Now is clicked
function addButton() {
    alert("Item has been added to cart")
}